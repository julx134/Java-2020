This project implements data abstraction to a stack data structure of distinct strings. 

The abstraction function is implemented as toString() method.

The rep invariant is implemented as repOk() method.

